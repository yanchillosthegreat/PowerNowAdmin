@page
@model IndexModel
@{
    Layout = "~/Pages/Shared/_Layout.cshtml";
}


<div class="wrapper-content">
    @Html.AntiForgeryToken()
    <div class="row">
        <div class="col text-center">
            <a class="btn btn-primary-orange m-b" style="width: 219px" href='@Url.Page("take/index")'>взять powerbank</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <p class="p-power-now-bold text-justify">Как взять Power Now?</p>
            <p class="p-power-now-normal">(в нашем пауер банке все виды разъёмов для зарядки)</p>
        </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <img src="~/css/patterns/city_1.png" class="img-responsive" style="width: 100%; height: auto;" />
            </div>
        </div>
    </div>


    @*<h1>
            Hi!
            @if (Model.IsAuthorized())
            {
                <span>@Model.GetName()</span>
            }
        </h1>
        <h5>You can Take device by pressing this beautifull big button</h5>
        <a class="btn btn-primary m-b btn-xl" href='@Url.Page("take/index")'>Take power bank</a>*@




